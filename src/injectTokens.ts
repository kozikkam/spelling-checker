export enum COMMON_PATHS {
    apiV1 = '/api/v1',
    document = '/documents',
}

export enum COMMON_IDENTIFIERS {
    IDocumentCommandService = 'IDocumentCommandService',
    IDocumentQueryService = 'IDocumentQueryService',
    ILogger = 'ILogger',
    IConfig = 'IConfig',
}

export enum COMMON_REPOSITORIES {
    IDocumentRepository = 'IDocumentRepository',
}

export enum COMMON_MODELS {
}
